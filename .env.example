# ======================================================================================
# DeepSearchStack - Definitive Environment Configuration
# ======================================================================================

# --- External Provider Toggles ---
ENABLE_GEMINI=true
ENABLE_GROQ=true

# --- API Keys ---
# CRITICAL: Revoke your old keys and replace these placeholders with NEW ones.
GEMINI_API_KEY="your_new_gemini_api_key_here"
GROQ_API_KEY="your_new_groq_api_key_here"

# --- Local Ollama Configuration ---
# This is the variable your custom entrypoint.sh will use to auto-pull models on first start.
# It is a comma-separated list.
OLLAMA_MODELS_PULL=gemma:2b,codellama

# This tells the llm-gateway which model to request from the running Ollama service.
# It MUST match one of the models you have pulled.
OLLAMA_DEFAULT_MODEL=gemma:2b

# --- Client & Proxy Configuration ---
BASE_URL=http://localhost
REVERSE_PROXY_BIND_IP=127.0.0.1

# --- Database Credentials ---
POSTGRES_DB=searchdb
POSTGRES_USER=searchuser
POSTGRES_PASSWORD=searchpass
