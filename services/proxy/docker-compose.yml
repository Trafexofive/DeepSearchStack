services:
  proxy:
    build: .
    ports:
      - "${PROXY_PORT:-8888}:8888"
    networks:
      - bridge-net
    restart: unless-stopped

networks:
  bridge-net:
    external: true
    name: infra_substrate-net
