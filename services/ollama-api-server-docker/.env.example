# ======================================================================================
# CHIMERA STACK - ENVIRONMENT CONFIGURATION
#
# Instructions:
# 1. Copy this file to .env
# 2. Modify the values below to suit your environment.
# ======================================================================================

# --- Traefik Reverse Proxy ---
# The domain for the API Gateway
TRAEFIK_API_DOMAIN=api.localhost

# The domain for the Chimera Frontend
TRAEFIK_FRONTEND_DOMAIN=localhost


# --- Docker Compose Network & Ports ---
# The external port for the API Gateway
GATEWAY_PORT=8000

# The external port for the Chimera Frontend
FRONTEND_PORT=3000

# The internal port for the main Ollama server (rarely needs changing)
OLLAMA_PORT=11434

# --- API Gateway Configuration ---
# The default model that workers will download and must have to be considered "ready"
REQUIRED_MODEL=gemma:2b

# The base image for the dynamically spawned workers
WORKER_IMAGE=ollama/ollama:latest

# The network your containers run on
CHIMERA_NETWORK=chimera-net

# --- Chimera Frontend Configuration ---
# A secret key for signing authentication tokens.
# Generate a strong one with: openssl rand -base64 32
NEXTAUTH_SECRET=YOUR_NEXTAUTH_SECRET_HERE

# The public URL of your application
NEXTAUTH_URL=http://localhost:3000

# The connection string for your database.
# For production, you would change this to point to a PostgreSQL or other database.
# Example: DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"
DATABASE_URL="file:./dev.db"
