services:
  sub-mq:
    build: .
    ports:
      - "${SUB_MQ_PORT:-8012}:8012"
    environment:
      - SUB_MQ_PORT=8012
      - REDIS_URL=${REDIS_URL:-redis://redis:6379/0}
    networks:
      - substrate-net

networks:
  substrate-net:
    external: true
    name: infra_substrate-net
