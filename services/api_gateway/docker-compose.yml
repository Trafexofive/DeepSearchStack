version: "3.8"
services:
  api_gateway:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_GATEWAY_PORT=8000
    volumes:
      - ./config:/app/config:ro
      - ./volumes/data:/app/data
    env_file:
      - ../../.env
