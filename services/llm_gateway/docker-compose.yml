version: "3.8"
services:
  llm_gateway:
    build: .
    ports:
      - "8002:8002"
    environment:
      - LLM_GATEWAY_PORT=8002
    volumes:
      - ./config:/app/config:ro
    env_file:
      - ../../.env
