version: "3.8"
services:
  blog_generator:
    build: .
    ports:
      - "8006:8006"
    environment:
      - BLOG_GENERATOR_PORT=8006
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - TRACKER_DB=/app/data/tracker.db
    volumes:
      - ./volumes/data:/app/data
    env_file:
      - ../../.env
